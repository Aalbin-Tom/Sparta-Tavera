	<script src="https://ajax.googleapis.com/ajax/libs/jquery.min.js">  </script>

<!-- breadcrumb-section -->
<div class="breadcrumb-section breadcrumb-bg">
    <div class="container">
        <div class="row">
            <div class="col-lg-8 offset-lg-2 text-center">
                <div class="breadcrumb-text">
                    <p>Tasty and sweety</p>
                    <h1>Cart</h1>
                </div>
            </div>
        </div>
    </div>
</div>
<!-- end breadcrumb section -->
<div class="cart-section mt-150 mb-150">
    <div class="container">
        <div class="row">
            <div class="col-lg-8 col-md-12">
                <div class="cart-table-wrap">
                    <table class="cart-table">
                        <thead class="cart-table-head">
                            <tr class="table-head-row">
                                <th class="product-remove"></th>
                                <th class="product-image">Product Image</th>
                                <th class="product-name">Name</th>
                                <th class="product-price">Price</th>
                                <th class="product-quantity">Quantity</th>
                            </tr>
                        </thead>
                        <tbody>
                            {{#each product}}
                            <div id='single'>
                            <tr class="table-body-row "  >

                                <td class="product-remove"><button <i class="far fa-window-close" onclick="removeproduct('{{this._id}}','{{this.item}}')" > </i></button> 
                                </td>
                                <td class="product-image"><img src="/productimage/{{this.product.images.[0]}}" alt="">
                                </td>
                                <td class="product-name">{{this.product.name}}</td>
                                <td class="product-price">{{this.product.price}}</td>


                                {{!-- <td> <button></button> class="product-quantity"><input type="number"
                                        placeholder="{{this.quantity}}" </td> --}}
                                <td><button class="cart-item-count mr-3"
                                        onclick="incQuantity('{{this._id}}','{{this.item}}','{{../user}}',-1)"
                                        style="border:none ;">-</button>
                                    <span id="{{this.item}}"> {{this.quantity}} </span>

                                    <button class="cart-item-count mr-3"
                                        onclick="incQuantity('{{this._id}}','{{this.item}}','{{../user}}',1)"
                                        style="border: none;">+</button>
                                </td>

                            </tr>
                            </div>
                            {{/each}}
                        </tbody>
                    </table>
                </div>
            </div>

            <div class="col-lg-4">
                <div class="total-section">
                    <table class="total-table">
                        <thead class="total-table-head">
                            <tr class="table-total-row">
                                <th>Total</th>
                                <th>Price</th>
                            </tr>
                        </thead>
                        <tbody>
                            <tr class="total-data">
                                <td><strong>Subtotal: </strong></td>
                                <td><span id="total">{{totalamount}}</span> Rs</td>
                            </tr>
                            <tr class="total-data">
                                <td><strong>Shipping: </strong></td>
                                <td>$45</td>
                            </tr>
                            <tr class="total-data">
                                <td><strong>Total: </strong></td>
                                <td>$545</td>
                            </tr>
                        </tbody>
                    </table>
                    <div class="cart-buttons">
                        <a href="cart.html" class="boxed-btn">Update Cart</a>
                        <a href="checkout.html" class="boxed-btn black">Check Out</a>
                    </div>
                </div>

                <div class="coupon-section">
                    <h3>Apply Coupon</h3>
                    <div class="coupon-form-wrap">
                        <form action="index.html">
                            <p><input type="text" placeholder="Coupon"></p>
                            <p><input type="submit" value="Apply"></p>
                        </form>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>
<!-- end cart -->


{{!--
<script>

    function incQuantity(cartId, productId, count) {
        console.log('ajax conected')
        $.ajax({
            url: '/changeproductquantity',
            data: {
                cart: cartId,
                product: productId,
                count: count
            },
            method: 'post',
            success: (response) => {
                alert(response)
            }
        })
    } 
</script>--}}